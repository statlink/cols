#useDynLib(pchc, .registration = TRUE)

importFrom("quadprog", solve.QP)
importFrom("Rfast", colsums)
importFrom("Rfast2", cls)

exportPattern("^[[:alpha:]]+")
exportPattern("^[^\\.]")

export(cls, pls, mvpls, pcls, mpls, mpcls)
